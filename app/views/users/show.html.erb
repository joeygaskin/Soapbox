<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Bio:</strong>
  <%= @user.bio %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<% if @user.posts.present? %>
hello
<% else %>
<%= @user.name %> doesn't have any post.
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', user_posts_path(@user) %>
